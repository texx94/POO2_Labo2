#ifndef STRING_HPP
#define STRING_HPP

#include <stdio.h>

/**
 * Permet d'implémenter une chaîne de caractères
 */
class String {
public:

private:
   char* value;   // Valeur de la chaîne de caractères
};

#endif
